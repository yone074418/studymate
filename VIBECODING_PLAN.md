# StudyMate vibicoding 项目开发计划

## 1. 我的开发目标

这个项目第一版的目标不是一开始就把页面做得很完整，而是先把后端核心流程跑通，再用前端把功能完整呈现出来。

我的开发策略是：

```text
先做后端基本流程
↓
稳定数据库和接口
↓
用 Swagger / Postman 测试每个接口
↓
冻结第一版接口字段
↓
再集中开发前端页面
↓
最后完成前后端联调
```

第一版要优先完成 StudyMate 的核心闭环：

```text
用户随手输入学习记录
↓
AI 自动整理
↓
用户确认保存
↓
系统保存学习记录
↓
系统统计学习数据
↓
系统分析薄弱点
↓
系统返回建议和安慰反馈
```

## 2. 项目原则

1. 后端优先，接口稳定后再做前端。
2. 先完成核心闭环，再考虑高级功能。
3. 每完成一个接口，就同步补充 Swagger / Knife4j 文档。
4. 每完成一个模块，就用 Swagger 或 Postman 测试完整流程。
5. 所有用户数据都必须按当前登录用户隔离。
6. 后端不能信任前端传入的 `user_id`，用户 ID 必须从 Token 中解析。
7. AI 结果必须允许用户确认和修改，不能直接强制保存。
8. 用户原始输入 `raw_content` 必须保存，AI 失败时也不能丢。
9. 统计逻辑以后端为准，前端只负责展示。
10. 第一版页面简洁、清晰、温暖，不做复杂后台系统风格。

## 3. 第一版范围

### 3.1 必须完成

| 模块 | 功能 |
|---|---|
| 用户模块 | 注册、登录、获取个人信息、修改个人信息 |
| 认证模块 | 密码加密、JWT 生成、JWT 校验、未登录拦截 |
| 学习记录模块 | 新增、列表、详情、修改、删除 |
| AI 整理模块 | 自然语言解析、结构化返回、mock 模式、AI 调用日志 |
| 薄弱点模块 | 从 AI 结果保存薄弱点、按用户统计薄弱点排行 |
| 统计模块 | 今日、本周、本月、累计时长、连续学习天数、方向占比、情绪趋势 |
| 文档测试 | Swagger / Knife4j 文档、Postman 或 Swagger 接口测试 |
| 前端页面 | 登录注册、首页、AI 记录、历史记录、统计、个人信息 |

### 3.2 暂时不做

1. AI 周报。
2. 复杂学习计划。
3. 面试题系统。
4. 简历管理。
5. 实习投递管理。
6. 好友、社区、排行榜。
7. 移动端 App。
8. Docker 完整部署。

## 4. 后端接口总清单

### 4.1 用户与认证接口

| 方法 | 路径 | 说明 | 测试重点 |
|---|---|---|---|
| POST | `/api/auth/register` | 用户注册 | 正常注册、重复用户名、重复邮箱、密码为空 |
| POST | `/api/auth/login` | 用户登录 | 登录成功、密码错误、用户不存在 |
| GET | `/api/user/profile` | 获取个人信息 | 携带 Token、未携带 Token、Token 过期 |
| PUT | `/api/user/profile` | 修改个人信息 | 正常修改、参数为空、未登录访问 |

### 4.2 AI 整理接口

| 方法 | 路径 | 说明 | 测试重点 |
|---|---|---|---|
| POST | `/api/ai/record/analyze` | AI 整理学习记录 | 正常输入、短输入、无学习时长、无情绪、AI 返回异常、mock 模式 |

### 4.3 学习记录接口

| 方法 | 路径 | 说明 | 测试重点 |
|---|---|---|---|
| POST | `/api/study-records` | 新增学习记录 | 正常保存、重复提交、参数为空、未登录 |
| GET | `/api/study-records` | 查询记录列表 | 当前用户数据、分页、日期筛选、分类筛选 |
| GET | `/api/study-records/{id}` | 查询记录详情 | 正常查询、不存在记录、越权访问 |
| PUT | `/api/study-records/{id}` | 修改学习记录 | 正常修改、不存在记录、越权修改 |
| DELETE | `/api/study-records/{id}` | 删除学习记录 | 正常删除、不存在记录、越权删除 |

### 4.4 统计接口

| 方法 | 路径 | 说明 | 测试重点 |
|---|---|---|---|
| GET | `/api/statistics/dashboard` | 首页仪表盘数据 | 今日、本周、连续天数、薄弱点摘要 |
| GET | `/api/statistics/trend` | 学习时长趋势 | 最近 7 天、最近 30 天、无数据默认值 |
| GET | `/api/statistics/category` | 学习方向占比 | 多分类统计、只统计当前用户 |
| GET | `/api/statistics/weak-points` | 薄弱点排行 | 最近 7 天、最近 30 天、按出现次数排序 |
| GET | `/api/statistics/emotion` | 情绪状态趋势 | 情绪分布、趋势数据、无数据默认值 |

## 5. 每日开发计划

### Day 1：项目初始化和数据库设计(已完成)

目标：把后端项目骨架和数据库设计先确定下来。

任务：

1. 创建 `studymate-backend` Spring Boot 项目。
2. 确认 Java 17、Spring Boot 3、Maven 版本。
3. 引入 Spring Web、MyBatis Plus、MySQL、Lombok、Validator、Swagger / Knife4j。
4. 创建基础包结构：`common`、`config`、`security`、`module`、`enums`。
5. 设计第一版数据库表：`user`、`study_record`、`study_record_category`、`weak_point`、`ai_call_log`、`study_category`。
6. 配置 `application-dev.yml`，连接本地 MySQL。
7. 写一个健康检查接口或基础测试接口。

当天要实现的接口：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/health` | 检查后端服务是否正常 |

当天测试：

1. 项目能正常启动。
2. MySQL 能正常连接。
3. Swagger / Knife4j 能正常访问。
4. `/api/health` 能返回统一响应格式。

当天完成标准：

```text
后端基础项目能启动，数据库能连接，接口文档能打开。
```

### Day 2：通用能力和用户表落地

目标：把后端基础设施补齐，为后续模块打底。

任务：

1. 实现统一返回类 `Result`。
2. 实现错误码枚举 `ResultCode`。
3. 实现全局异常处理 `GlobalExceptionHandler`。
4. 实现业务异常 `BusinessException`。
5. 配置参数校验。
6. 创建 `User` Entity、`UserMapper`、`UserService` 基础结构。
7. 创建用户表 SQL 并初始化数据库。
8. 准备密码加密方案。

当天要实现的接口：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/health` | 使用统一返回格式 |

当天测试：

1. 正常响应返回 `code/message/data`。
2. 参数校验异常能返回友好错误。
3. 业务异常能被统一捕获。
4. 数据库 `user` 表可以正常访问。

当天完成标准：

```text
统一响应、统一异常、参数校验、用户表访问全部可用。
```

### Day 3：用户注册登录(已完成)

目标：完成最基础的用户进入系统能力。

任务：

1. 创建 `RegisterDTO`、`LoginDTO`、`UserInfoVO`。
2. 实现用户注册接口。
3. 注册时校验用户名和邮箱不能重复。
4. 密码加密存储。
5. 实现用户登录接口。
6. 登录成功返回 JWT。
7. 登录失败返回明确错误。
8. Swagger 文档补充请求参数和响应字段。

当天要实现的接口：

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录 |

当天测试：

1. 正常注册。
2. 重复用户名注册。
3. 重复邮箱注册。
4. 密码为空。
5. 正常登录。
6. 密码错误。
7. 登录成功返回 Token。

当天完成标准：

```text
用户可以注册和登录，密码不会明文保存，登录后能拿到 Token。
```

### Day 4：JWT 鉴权和个人信息(已完成)

目标：让受保护接口只能由登录用户访问。

任务：

1. 实现 `JwtUtil`。
2. 实现 JWT 过滤器。
3. 配置 Spring Security。
4. 实现当前登录用户获取逻辑。
5. 实现获取个人信息接口。
6. 实现修改个人信息接口。
7. 配置 401、403 返回格式。
8. 配置跨域。

当天要实现的接口：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/user/profile` | 获取当前用户信息 |
| PUT | `/api/user/profile` | 修改当前用户信息 |

当天测试：

1. 携带 Token 可以访问个人信息。
2. 不带 Token 返回 401。
3. 错误 Token 返回 401。
4. 修改昵称、目标岗位、每日目标学习时长。
5. 返回结果不包含 `password` 字段。

当天完成标准：

```text
JWT 鉴权流程跑通，后续所有用户数据接口都能基于当前登录用户处理。
```

### Day 5：学习记录表和新增接口（已完成）

目标：让用户可以保存一条学习记录。

任务：

1. 创建 `study_record`、`study_record_category`、`weak_point` 表。
2. 创建 `StudyRecord` Entity。
3. 创建 `StudyRecordCreateDTO`。
4. 创建 `StudyRecordVO`、`StudyRecordDetailVO`。
5. 实现新增学习记录接口。
6. 保存 `raw_content`、学习日期、学习时长、学习内容、情绪状态、明日计划、AI 总结、安慰反馈。
7. 同步保存学习方向。
8. 同步保存薄弱点。
9. 做简单重复提交处理。

当天要实现的接口：

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/study-records` | 新增学习记录 |

当天测试：

1. 正常新增学习记录。
2. 未登录新增返回 401。
3. `rawContent` 为空返回参数错误。
4. 同一天允许多条不同记录。
5. 完全相同内容不重复保存。
6. 学习方向和薄弱点能同步保存。

当天完成标准：

```text
登录用户可以保存完整学习记录，并且记录归属于当前用户。
```

### Day 6：学习记录查询、详情、修改、删除（完成）

目标：完成学习记录 CRUD 闭环。

任务：

1. 实现学习记录列表查询。
2. 支持分页、日期、学习方向、情绪状态筛选。
3. 实现学习记录详情查询。
4. 实现学习记录修改。
5. 修改时同步处理学习方向和薄弱点。
6. 实现学习记录删除。
7. 删除后不参与统计。
8. 所有操作都校验记录归属。

当天要实现的接口：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/study-records` | 查询学习记录列表 |
| GET | `/api/study-records/{id}` | 查询学习记录详情 |
| PUT | `/api/study-records/{id}` | 修改学习记录 |
| DELETE | `/api/study-records/{id}` | 删除学习记录 |

当天测试：

1. 查询自己的记录列表。
2. 查询记录详情。
3. 修改自己的记录。
4. 删除自己的记录。
5. 查询不存在记录。
6. 修改不存在记录。
7. 删除不存在记录。
8. 不能查询、修改、删除其他用户记录。

当天完成标准：

```text
学习记录 CRUD 完整可用，用户数据隔离测试通过。
```

### Day 7：AI 整理模块 mock 模式（已完成）

目标：先不依赖真实 AI，把接口流程跑通。

任务：

1. 创建 `AiAnalyzeRequestDTO`。
2. 创建 `AiAnalyzeResultVO`。
3. 创建 `AiRecordController`。
4. 创建 `AiRecordAnalyzeService`。
5. 实现 mock AI 返回。
6. 返回固定结构：学习时长、学习方向、学习内容、薄弱点、情绪状态、明日计划、AI 总结、安慰反馈。
7. 创建 `ai_call_log` 表和基础记录逻辑。
8. Swagger 文档补充 AI 输出字段。

当天要实现的接口：

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/ai/record/analyze` | AI 整理学习记录，先使用 mock |

当天测试：

1. 正常自然语言输入。
2. 输入内容为空。
3. 输入内容很短。
4. mock 返回字段完整。
5. AI 调用日志能保存。
6. 未登录访问返回 401。

当天完成标准：

```text
不用真实 AI，也能完成“输入文本 → 返回结构化结果”的接口流程。
```

### Day 8：真实 AI 接入和异常处理

目标：把 AI 模块从 mock 扩展到真实调用，同时保证失败可控。

任务：

1. 封装 `AiClient`。
2. 编写 `StudyRecordPromptBuilder`。
3. Prompt 明确要求只返回 JSON。
4. 实现 AI JSON 解析。
5. 处理非 JSON 返回。
6. 处理字段缺失。
7. 处理超时、限流、网络异常。
8. 保存 AI 原始响应和错误日志。
9. 增加配置开关：mock 模式 / 真实 AI 模式。

当天要实现的接口：

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/ai/record/analyze` | AI 整理学习记录，支持 mock 和真实 AI |

当天测试：

1. 正常真实 AI 返回。
2. AI 返回非 JSON。
3. AI 字段缺失。
4. AI 请求失败。
5. AI 超时。
6. mock 模式仍可用。
7. 原始输入不会丢失。

当天完成标准：

```text
AI 整理接口可用，并且失败时不会影响用户继续使用系统。
```

### Day 9：统计分析接口

目标：完成首页和统计页需要的后端数据。

任务：

1. 创建 `StatisticsController`。
2. 创建 `StatisticsService`。
3. 实现今日学习时长统计。
4. 实现本周、本月、累计学习时长统计。
5. 实现连续学习天数统计。
6. 实现最近 7 天学习趋势。
7. 实现学习方向占比。
8. 实现薄弱点排行。
9. 实现情绪状态趋势。
10. 实现首页仪表盘聚合接口。

当天要实现的接口：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/statistics/dashboard` | 首页仪表盘数据 |
| GET | `/api/statistics/trend` | 学习时长趋势 |
| GET | `/api/statistics/category` | 学习方向占比 |
| GET | `/api/statistics/weak-points` | 薄弱点排行 |
| GET | `/api/statistics/emotion` | 情绪状态趋势 |

当天测试：

1. 有数据时统计正确。
2. 无数据时返回默认值。
3. 只统计当前登录用户。
4. 删除记录后不再参与统计。
5. 多学习方向记录能正确统计。
6. 薄弱点按出现次数排序。

当天完成标准：

```text
首页和统计页需要的所有后端数据都能通过接口返回。
```

### Day 10：后端整体测试和接口冻结

目标：前端开发前，把接口稳定下来。

任务：

1. 全量检查接口路径。
2. 全量检查请求参数。
3. 全量检查响应字段。
4. 全量检查错误码。
5. 补齐 Swagger / Knife4j 文档。
6. 用 Swagger 或 Postman 跑完整流程。
7. 修复明显问题。
8. 整理接口清单。
9. 标记第一版接口冻结。

完整流程测试：

```text
注册
↓
登录
↓
获取个人信息
↓
AI 整理学习记录
↓
确认保存学习记录
↓
查询历史记录
↓
查询记录详情
↓
修改记录
↓
查看统计数据
↓
删除记录
↓
确认统计数据变化
```

当天完成标准：

```text
后端核心接口完整跑通，接口字段基本冻结，前端可以开始开发。
```

### Day 11：前端项目初始化

目标：搭建前端基础工程和请求体系。

任务：

1. 创建 `studymate-frontend` Vue 3 + Vite 项目。
2. 引入 TypeScript、Vue Router、Pinia、Axios、Element Plus、ECharts。
3. 创建前端目录：`api`、`components`、`router`、`stores`、`types`、`utils`、`views`。
4. 封装 Axios 请求实例。
5. 请求拦截器携带 Token。
6. 响应拦截器统一处理 401 和错误提示。
7. 配置基础路由。

当天页面：

1. 登录页骨架。
2. 注册页骨架。
3. 首页布局骨架。

当天测试：

1. 前端项目能启动。
2. 路由能跳转。
3. Axios 能请求后端。
4. Token 能保存和携带。
5. 401 能跳转登录页。

当天完成标准：

```text
前端工程可运行，请求后端的基础能力可用。
```

### Day 12：登录注册和首页

目标：完成用户进入系统和首页展示。

任务：

1. 完成注册页面。
2. 完成登录页面。
3. 登录成功后保存 Token。
4. 登录成功跳转首页。
5. 首页调用 `/api/statistics/dashboard`。
6. 首页展示今日学习状态、本周时长、连续学习天数、薄弱点摘要、AI 鼓励语。
7. 首页突出 AI 随手记录入口。

当天测试：

1. 正常注册。
2. 正常登录。
3. 登录失败提示。
4. 退出登录。
5. 首页能显示统计数据。
6. 未登录访问首页跳转登录页。

当天完成标准：

```text
用户可以从注册登录进入系统，并看到首页核心数据。
```

### Day 13：AI 随手记录和保存流程

目标：完成产品最核心的前端使用流程。

任务：

1. 完成 AI 随手记录页面。
2. 页面中提供明显的大输入框。
3. 调用 `/api/ai/record/analyze`。
4. 展示 AI 整理结果。
5. 支持用户修改 AI 结果。
6. 点击确认保存时调用 `/api/study-records`。
7. 处理 AI 加载状态。
8. 处理 AI 调用失败和保存失败。

当天测试：

1. 输入自然语言后能调用 AI 整理。
2. AI 结果能正确展示。
3. AI 结果可以修改。
4. 可以确认保存。
5. 保存成功后跳转历史记录或首页。
6. AI 失败时原始输入仍保留。

当天完成标准：

```text
用户可以完成“随手输入 → AI 整理 → 用户确认 → 保存记录”的核心流程。
```

### Day 14：历史记录和统计页面

目标：完成查看和复盘能力。

任务：

1. 完成历史记录列表页。
2. 支持日期、学习方向、情绪状态筛选。
3. 完成记录详情页或详情弹窗。
4. 支持编辑记录。
5. 支持删除记录。
6. 完成统计页面。
7. 展示学习时长趋势、方向占比、薄弱点排行、情绪趋势。
8. 图表数据全部来自后端。

当天测试：

1. 历史记录能分页展示。
2. 详情能打开。
3. 编辑后数据更新。
4. 删除后列表和统计变化。
5. 图表能正常展示。
6. 无数据状态显示正常。

当天完成标准：

```text
用户可以回顾历史记录，也可以查看自己的学习统计和薄弱点。
```

### Day 15：前后端完整联调和项目收尾

目标：让第一版 MVP 可以完整演示。

任务：

1. 跑完整用户流程。
2. 修复前后端字段不一致问题。
3. 修复登录态、Token 过期、错误提示问题。
4. 检查页面 loading 状态。
5. 检查空数据状态。
6. 检查接口权限控制。
7. 整理 README 或运行说明。
8. 整理项目演示流程。

最终演示流程：

```text
注册账号
↓
登录系统
↓
进入首页
↓
输入学习记录
↓
AI 整理
↓
用户修改并确认保存
↓
查看历史记录
↓
查看统计图表
↓
验证不能访问其他用户数据
```

当天完成标准：

```text
StudyMate 第一版 MVP 可以完整跑通，并且适合用于项目展示。
```

## 6. 每天的进度记录模板

每天开发结束后，可以按下面格式记录：

```markdown
## Day X 开发记录

### 今日目标

- 

### 已完成

- 

### 今日实现接口

| 方法 | 路径 | 状态 | 备注 |
|---|---|---|---|
|  |  | 已完成 / 待修复 |  |

### 今日测试

- [ ] 正常请求
- [ ] 参数为空
- [ ] 参数格式错误
- [ ] 未登录访问
- [ ] Token 过期
- [ ] 不存在数据
- [ ] 越权访问

### 遇到的问题

- 

### 明天计划

- 
```

## 7. 模块完成检查清单

### 7.1 用户认证模块

- [ ] 用户注册接口完成。
- [ ] 用户登录接口完成。
- [ ] 密码加密存储。
- [ ] JWT 生成完成。
- [ ] JWT 校验完成。
- [ ] 未登录返回 401。
- [ ] 接口返回不包含密码。
- [ ] Swagger 文档完整。
- [ ] Swagger / Postman 测试通过。

### 7.2 学习记录模块

- [ ] 新增记录完成。
- [ ] 查询列表完成。
- [ ] 查询详情完成。
- [ ] 修改记录完成。
- [ ] 删除记录完成。
- [ ] 学习方向保存完成。
- [ ] 薄弱点保存完成。
- [ ] 原始输入保存完成。
- [ ] 越权访问拦截完成。
- [ ] 重复提交处理完成。

### 7.3 AI 整理模块

- [ ] mock AI 模式完成。
- [ ] 真实 AI 接入完成。
- [ ] Prompt 单独管理。
- [ ] AI 只返回 JSON 的约束完成。
- [ ] AI JSON 解析完成。
- [ ] AI 异常处理完成。
- [ ] AI 调用日志完成。
- [ ] AI 失败时原始输入不丢失。
- [ ] AI 结果字段完整。

### 7.4 统计模块

- [ ] 今日学习时长。
- [ ] 本周学习时长。
- [ ] 本月学习时长。
- [ ] 累计学习时长。
- [ ] 连续学习天数。
- [ ] 学习方向占比。
- [ ] 最近 7 天趋势。
- [ ] 薄弱点排行。
- [ ] 情绪状态趋势。
- [ ] 无数据默认返回。
- [ ] 只统计当前用户。

### 7.5 前端模块

- [ ] 登录注册页面。
- [ ] 首页仪表盘。
- [ ] AI 随手记录页面。
- [ ] AI 整理结果展示和编辑。
- [ ] 确认保存流程。
- [ ] 历史记录列表。
- [ ] 记录详情。
- [ ] 编辑删除记录。
- [ ] 统计图表。
- [ ] Token 失效处理。
- [ ] loading 和错误提示。

## 8. 第一版完成标准

第一版完成时，必须满足：

1. 用户可以注册登录。
2. 用户可以输入自然语言学习记录。
3. 后端可以调用 AI 或 mock AI 整理记录。
4. AI 结果可以返回结构化数据。
5. 用户可以确认并保存学习记录。
6. 用户可以查看历史记录。
7. 用户可以修改和删除自己的记录。
8. 系统可以统计学习时长。
9. 系统可以统计学习方向占比。
10. 系统可以统计薄弱点排行。
11. 系统可以统计情绪状态趋势。
12. 系统可以返回安慰反馈。
13. 用户不能访问其他用户的数据。
14. 后端接口文档完整。
15. 前端页面可以完整跑通核心流程。

## 9. 我的节奏控制方法

开发时每天只问三个问题：

1. 今天要完成哪个模块？
2. 今天新增或修改了哪些接口？
3. 今天这些接口有没有测完？

每个接口完成后必须留下三样东西：

1. 代码实现。
2. Swagger / Knife4j 文档。
3. Swagger 或 Postman 测试结果。

只要每天围绕这三件事推进，项目就不会失控。
