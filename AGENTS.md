# AGENTS.md

# StudyMate：AI 学习复盘与 Java 实习备考陪伴系统开发规范

## 1. 项目概述

StudyMate 是一个面向 Java 实习备考人群的 AI 学习复盘与情绪陪伴系统。

用户每天学习结束后，只需要输入一段自然语言学习记录，系统通过 AI 自动整理学习内容、提取薄弱点、分析情绪状态，并生成明日建议和安慰反馈。

本项目第一版重点实现完整闭环：

    用户随手输入学习记录
        ↓
    AI 自动整理
        ↓
    用户确认保存
        ↓
    系统保存学习记录
        ↓
    系统统计学习数据
        ↓
    系统分析薄弱点
        ↓
    系统返回建议和安慰

---

## 2. 项目开发策略

本项目采用后端优先开发策略。

第一阶段优先完成后端核心功能，包括：

1. 数据库表设计。
2. 用户注册登录。
3. JWT 鉴权。
4. 学习记录 CRUD。
5. AI 学习记录解析。
6. 薄弱点保存和统计。
7. 学习数据统计。
8. Swagger / Knife4j 接口文档。
9. Postman 或 Swagger 接口测试。

后端接口基本稳定后，再集中开发前端页面。

开发顺序为：

    后端基础框架
        ↓
    用户认证模块
        ↓
    学习记录模块
        ↓
    AI 整理模块
        ↓
    统计分析模块
        ↓
    后端接口测试
        ↓
    接口文档整理
        ↓
    前端页面开发
        ↓
    前后端联调

---

## 3. 技术栈约定

## 3.1 后端技术栈

| 技术 | 说明 |
|---|---|
| Java 17 | 后端开发语言 |
| Spring Boot 3 | 后端核心框架 |
| Spring Web | RESTful API |
| Spring Security | 登录认证和权限控制 |
| JWT | 前后端分离 Token 鉴权 |
| MyBatis Plus | 数据库 CRUD |
| MySQL 8 | 主数据库 |
| Redis | 缓存、限流、Token 黑名单，第一版可预留 |
| Lombok | 简化实体类代码 |
| Hibernate Validator | 参数校验 |
| Swagger / Knife4j | 接口文档 |
| Maven | 依赖管理 |

---

## 3.2 前端技术栈

| 技术 | 说明 |
|---|---|
| Vue 3 | 前端核心框架 |
| Vite | 构建工具 |
| TypeScript | 类型安全 |
| Vue Router | 路由管理 |
| Pinia | 状态管理 |
| Axios | 请求后端接口 |
| Element Plus | UI 组件库 |
| ECharts | 数据图表展示 |

---

## 4. 项目核心模块

## 4.1 用户模块

负责用户注册、登录、个人信息和认证相关功能。

核心功能：

1. 用户注册。
2. 用户登录。
3. 密码加密。
4. JWT 生成和校验。
5. 获取当前登录用户。
6. 修改个人信息。

---

## 4.2 学习记录模块

负责用户每日学习记录的保存和管理。

核心功能：

1. 新增学习记录。
2. 查询学习记录列表。
3. 查询学习记录详情。
4. 修改学习记录。
5. 删除学习记录。
6. 保存用户原始输入。
7. 保存 AI 整理结果。

---

## 4.3 AI 整理模块

负责调用 AI，将用户自然语言学习记录整理成结构化数据。

AI 需要输出：

1. 学习时长。
2. 学习方向。
3. 学习内容。
4. 薄弱点。
5. 情绪状态。
6. 明日计划。
7. AI 总结。
8. 安慰反馈。

AI 返回结果必须经过后端解析，并返回给前端确认。

---

## 4.4 薄弱点模块

负责保存和统计用户的薄弱点。

核心功能：

1. 从 AI 结果中保存薄弱点。
2. 按用户统计薄弱点。
3. 按最近 7 天、30 天统计薄弱点。
4. 支持后续扩展“已解决”状态。

---

## 4.5 统计模块

负责首页和统计页面的数据计算。

核心功能：

1. 今日学习时长。
2. 本周学习时长。
3. 本月学习时长。
4. 累计学习时长。
5. 连续学习天数。
6. 学习方向占比。
7. 薄弱点排行。
8. 情绪状态趋势。

---

## 5. 后端项目结构规范

后端采用单体分层架构，第一版不拆微服务。

    studymate-backend
    ├── src
    │   ├── main
    │   │   ├── java
    │   │   │   └── com
    │   │   │       └── studymate
    │   │   │           ├── common
    │   │   │           ├── config
    │   │   │           ├── security
    │   │   │           ├── module
    │   │   │           │   ├── user
    │   │   │           │   ├── study
    │   │   │           │   ├── ai
    │   │   │           │   ├── statistics
    │   │   │           │   └── category
    │   │   │           └── enums
    │   │   └── resources
    │   │       ├── application.yml
    │   │       ├── application-dev.yml
    │   │       ├── application-prod.yml
    │   │       └── mapper
    │   └── test

---

## 6. 后端分层规范

## 6.1 Controller 层

Controller 层只负责：

1. 接收请求。
2. 参数校验。
3. 调用 Service。
4. 返回统一响应结果。

Controller 层禁止写复杂业务逻辑。

错误示例：

    在 Controller 中直接写数据库查询、AI 调用、复杂统计逻辑。

正确做法：

    Controller 接收参数后，调用对应 Service 方法。

---

## 6.2 Service 层

Service 层负责核心业务逻辑。

主要包括：

1. 用户注册登录逻辑。
2. 学习记录保存逻辑。
3. AI 结果解析逻辑。
4. 薄弱点保存逻辑。
5. 统计计算逻辑。
6. 用户数据权限校验。

---

## 6.3 Mapper 层

Mapper 层只负责数据库访问。

要求：

1. 简单 CRUD 使用 MyBatis Plus。
2. 复杂统计查询可以写 Mapper XML。
3. 不在 Mapper 层写业务判断。
4. 查询用户数据时必须带上 user_id 条件。

---

## 6.4 Entity 层

Entity 对应数据库表结构。

要求：

1. Entity 不直接返回给前端。
2. Entity 不写复杂业务逻辑。
3. Entity 字段要和数据库字段保持清晰对应。

---

## 6.5 DTO 层

DTO 用于接收前端请求参数。

常见 DTO：

1. LoginDTO。
2. RegisterDTO。
3. StudyRecordCreateDTO。
4. StudyRecordUpdateDTO。
5. StudyRecordQueryDTO。
6. AiAnalyzeRequestDTO。

---

## 6.6 VO 层

VO 用于返回前端展示数据。

常见 VO：

1. UserInfoVO。
2. StudyRecordVO。
3. StudyRecordDetailVO。
4. AiAnalyzeResultVO。
5. DashboardVO。
6. StudyTrendVO。
7. WeakPointRankVO。

---

## 7. 前端项目结构规范

前端在后端接口稳定后开发。

    studymate-frontend
    ├── src
    │   ├── api
    │   ├── assets
    │   ├── components
    │   ├── router
    │   ├── stores
    │   ├── types
    │   ├── utils
    │   └── views

---

## 8. 前端开发规范

## 8.1 API 请求规范

所有接口请求统一放在 api 目录。

要求：

1. 不在页面组件中直接写 Axios 原始请求。
2. Axios 需要统一封装请求拦截器和响应拦截器。
3. Token 统一在请求拦截器中携带。
4. 401 响应统一跳转登录页。
5. 错误提示统一处理。

---

## 8.2 页面组件规范

页面组件放在 views 目录。

通用组件放在 components 目录。

要求：

1. 页面组件负责页面布局和交互。
2. 通用组件负责复用展示。
3. 复杂逻辑不要全部堆在页面里。
4. 图表组件单独封装。
5. AI 整理结果卡片单独封装。

---

## 8.3 前端业务原则

前端主要负责展示和交互，不做复杂业务计算。

要求：

1. 学习统计数据由后端返回。
2. 薄弱点排行由后端返回。
3. 情绪趋势由后端返回。
4. 前端只负责展示图表和列表。
5. 不在前端重复实现后端统计逻辑。

---

## 9. 接口规范

## 9.1 统一返回格式

所有后端接口必须使用统一返回格式。

成功返回：

    {
      "code": 200,
      "message": "success",
      "data": {}
    }

失败返回：

    {
      "code": 400,
      "message": "参数错误",
      "data": null
    }

未登录返回：

    {
      "code": 401,
      "message": "请先登录",
      "data": null
    }

无权限返回：

    {
      "code": 403,
      "message": "无权限访问",
      "data": null
    }

---

## 9.2 接口路径规范

接口路径使用 RESTful 风格。

用户相关：

    POST /api/auth/register
    POST /api/auth/login
    GET  /api/user/profile
    PUT  /api/user/profile

学习记录相关：

    POST   /api/study-records
    GET    /api/study-records
    GET    /api/study-records/{id}
    PUT    /api/study-records/{id}
    DELETE /api/study-records/{id}

AI 相关：

    POST /api/ai/record/analyze

统计相关：

    GET /api/statistics/dashboard
    GET /api/statistics/trend
    GET /api/statistics/category
    GET /api/statistics/weak-points
    GET /api/statistics/emotion

---

## 9.3 接口文档要求

后端每完成一个接口，都需要同步完善 Swagger / Knife4j 文档。

接口文档必须包含：

1. 接口说明。
2. 请求方法。
3. 请求路径。
4. 请求参数。
5. 响应字段。
6. 错误情况。
7. 是否需要登录。

---

## 10. 数据安全规范

## 10.1 用户数据隔离

学习记录、薄弱点、统计数据都属于用户个人数据。

必须遵守：

1. 用户只能查看自己的学习记录。
2. 用户只能修改自己的学习记录。
3. 用户只能删除自己的学习记录。
4. 用户只能查看自己的统计数据。
5. 后端不能信任前端传入的 user_id。
6. 当前用户 ID 必须从 Token 中解析。

---

## 10.2 密码安全

要求：

1. 用户密码不能明文存储。
2. 必须使用加密算法存储密码。
3. 登录时比较加密后的密码。
4. 接口返回结果中不能包含 password 字段。

---

## 10.3 AI 数据安全

AI 调用时需要注意用户隐私。

要求：

1. 只传递必要的学习记录内容。
2. 不传递用户密码、邮箱等敏感信息。
3. AI 原始响应可以记录，但仅用于调试和排查问题。
4. AI 调用日志不能暴露给普通用户。
5. 用户删除学习记录后，相关薄弱点也应同步处理。

---

## 11. AI 模块开发规范

## 11.1 AI 调用封装

AI 调用必须封装在 AiClient 中。

推荐调用链路：

    AiRecordController
        ↓
    AiRecordAnalyzeService
        ↓
    StudyRecordPromptBuilder
        ↓
    AiClient
        ↓
    AI 模型服务

禁止在 Controller 中直接调用 AI 接口。

---

## 11.2 AI Prompt 管理

AI 提示词必须单独管理。

建议放在：

    module/ai/prompt/StudyRecordPromptBuilder.java

Prompt 要明确要求 AI：

1. 只返回 JSON。
2. 不要输出多余解释。
3. 必须从固定学习方向中选择。
4. 必须从固定情绪状态中选择。
5. 安慰反馈不能打击用户。
6. 建议要具体、轻量、可执行。
7. 用户疲惫时优先降低任务量。

---

## 11.3 AI 输出字段

AI 标准输出字段：

| 字段 | 说明 |
|---|---|
| durationMinutes | 学习时长，单位分钟 |
| categories | 学习方向数组 |
| studyContent | 学习内容总结 |
| weakPoints | 薄弱点数组 |
| emotionStatus | 情绪状态 |
| tomorrowPlan | 明日计划 |
| aiSummary | AI 总结 |
| aiComfort | 安慰反馈 |

---

## 11.4 AI 异常处理

AI 可能出现以下问题：

1. 返回不是 JSON。
2. 字段缺失。
3. 请求超时。
4. 接口限流。
5. 网络异常。
6. 返回内容不符合要求。

处理方式：

1. 后端必须捕获异常。
2. 保存 AI 调用日志。
3. 返回友好错误提示。
4. 保留用户原始输入。
5. 前端后续提供重新生成按钮。
6. 字段缺失时可以使用默认值。

---

## 12. 数据库规范

## 12.1 表设计原则

数据库设计要清晰，避免过度复杂。

第一版主要表：

1. user。
2. study_record。
3. study_record_category。
4. weak_point。
5. ai_call_log。
6. study_category。

---

## 12.2 字段命名规范

数据库字段使用下划线命名。

示例：

    user_id
    raw_content
    duration_minutes
    emotion_status
    create_time
    update_time

Java 字段使用小驼峰命名。

示例：

    userId
    rawContent
    durationMinutes
    emotionStatus
    createTime
    updateTime

---

## 12.3 时间字段规范

每张业务表建议包含：

1. create_time。
2. update_time。

涉及逻辑删除时可以加入：

1. deleted。

---

## 13. 代码风格规范

## 13.1 Java 代码风格

要求：

1. 类名使用大驼峰命名。
2. 方法名使用小驼峰命名。
3. 变量名使用小驼峰命名。
4. 常量名使用全大写加下划线。
5. 方法职责要单一。
6. 单个方法不要过长。
7. 复杂业务逻辑要写注释。
8. 不要在代码中硬编码魔法值。
9. 枚举值使用 Enum 管理。
10. 不要把 Entity 直接返回给前端。

---

## 13.2 前端代码风格

要求：

1. 组件名使用大驼峰命名。
2. 文件名尽量语义清晰。
3. API 方法名要和业务含义一致。
4. TypeScript 类型单独放到 types 目录。
5. 页面逻辑不要过度堆积。
6. 公共方法放到 utils 目录。
7. 加载状态和错误状态必须处理。
8. 页面风格保持简洁、温暖、低压迫感。

---

## 14. 测试要求

## 14.1 后端接口测试

后端优先开发阶段，必须重点测试接口。

测试工具：

1. Swagger / Knife4j。
2. Postman。
3. JUnit，可后续补充。

每个接口至少测试：

1. 正常请求。
2. 参数为空。
3. 参数格式错误。
4. 未登录访问。
5. Token 过期。
6. 操作不存在的数据。
7. 操作其他用户的数据。

---

## 14.2 用户模块测试

需要测试：

1. 用户正常注册。
2. 重复用户名注册。
3. 重复邮箱注册。
4. 密码为空。
5. 登录成功。
6. 登录密码错误。
7. Token 正常访问接口。
8. 未携带 Token 访问受保护接口。

---

## 14.3 学习记录模块测试

需要测试：

1. 新增学习记录。
2. 查询学习记录列表。
3. 查询学习记录详情。
4. 修改学习记录。
5. 删除学习记录。
6. 查询不存在的记录。
7. 修改其他用户的记录。
8. 删除其他用户的记录。
9. 同一天多条记录。
10. 完全相同内容重复提交。

---

## 14.4 AI 模块测试

需要测试：

1. 正常自然语言输入。
2. 输入内容很短。
3. 输入内容没有学习时长。
4. 输入内容没有明确情绪。
5. 输入内容包含多个学习方向。
6. AI 返回非 JSON。
7. AI 返回字段缺失。
8. AI 接口超时。
9. AI 接口调用失败。
10. mock 模式是否可用。

---

## 14.5 统计模块测试

需要测试：

1. 今日学习时长。
2. 本周学习时长。
3. 本月学习时长。
4. 累计学习时长。
5. 连续学习天数。
6. 学习方向占比。
7. 薄弱点排行。
8. 情绪状态趋势。
9. 无学习记录时的默认返回。
10. 只统计当前用户数据。

---

## 15. 前端测试要求

前端后置开发时，需要测试完整使用流程。

核心流程：

1. 用户注册。
2. 用户登录。
3. 进入首页。
4. 输入自然语言学习记录。
5. 调用 AI 整理。
6. 展示 AI 整理结果。
7. 用户确认保存。
8. 查看历史记录。
9. 查看统计数据。
10. Token 失效后跳转登录页。

---

## 16. 开发注意事项

## 16.1 第一版不要过度扩展

第一版只做核心闭环。

必须做：

1. AI 随手记录。
2. AI 整理。
3. 用户确认保存。
4. 历史记录。
5. 学习统计。
6. 薄弱点分析。
7. 安慰建议。

暂时不做：

1. 社区功能。
2. 好友功能。
3. 排行榜。
4. 移动端 App。
5. 复杂学习路线生成。
6. 简历系统。
7. 实习投递系统。

这些功能可以放到后续版本。

---

## 16.2 后端接口先稳定再做前端

本项目开发重点是先做好后端。

后端接口稳定前，不要过早投入大量前端页面开发。

前端可以提前准备：

1. 页面结构草图。
2. 路由设计。
3. UI 风格。
4. 接口字段确认。

但不要在接口未定时写大量页面逻辑。

---

## 16.3 保留用户原始输入

无论 AI 是否成功解析，都要保留 raw_content。

原因：

1. AI 失败后可以重新解析。
2. 用户可以回看自己原始记录。
3. 后续可以优化 AI 提示词。
4. AI 解析错误时方便对照修改。

---

## 16.4 AI 建议不能增加压力

AI 安慰反馈和明日建议必须符合产品定位。

要求：

1. 不要批评用户。
2. 不要制造焦虑。
3. 不要说用户不努力。
4. 不要给过重任务。
5. 用户疲惫时建议降低任务量。
6. 建议要小、具体、可完成。

示例：

    明天可以先整理一张 Redis AOF 和 RDB 对比表。

不要写成：

    你应该尽快全面掌握 Redis 所有知识点，否则会影响实习面试。

---

## 16.5 统计数据以后端为准

前端不要自己计算复杂统计。

原因：

1. 逻辑容易重复。
2. 前后端结果可能不一致。
3. 后端更适合统一处理时间范围和用户权限。
4. 后续缓存和优化也更方便。

---

## 16.6 mock AI 模式

开发早期可以使用 mock AI 返回，避免真实 AI 接口影响开发。

mock 返回示例：

    durationMinutes: 120
    categories: Redis
    studyContent: 今天主要学习了 Redis 持久化
    weakPoints: AOF 和 RDB 对比
    emotionStatus: 有点累
    tomorrowPlan: 整理 Redis 持久化对比表
    aiSummary: 今天学习了 Redis 持久化相关内容
    aiComfort: 能发现自己哪里不懂，本身就是一种进步

---

## 17. Git 提交规范

提交信息建议使用以下格式：

| 类型 | 说明 |
|---|---|
| feat | 新功能 |
| fix | 修复问题 |
| docs | 文档修改 |
| style | 代码格式修改 |
| refactor | 代码重构 |
| test | 测试相关 |
| chore | 构建、配置、依赖等修改 |

示例：

    feat: 完成用户注册登录接口
    feat: 新增 AI 学习记录解析接口
    fix: 修复学习记录越权访问问题
    docs: 更新接口文档
    refactor: 优化统计服务结构

---

## 18. 完成标准

第一版完成标准：

1. 用户可以注册登录。
2. 用户可以输入自然语言学习记录。
3. 后端可以调用 AI 整理记录。
4. AI 结果可以返回结构化数据。
5. 用户可以保存学习记录。
6. 用户可以查看历史记录。
7. 用户可以修改和删除自己的记录。
8. 系统可以统计学习时长。
9. 系统可以统计学习方向占比。
10. 系统可以统计薄弱点排行。
11. 系统可以返回安慰反馈。
12. 用户不能访问其他用户的数据。
13. 后端接口文档完整。
14. 前端页面可以完整跑通核心流程。

---

## 19. 项目核心原则

本项目开发时始终遵守以下原则：

1. 后端优先，接口稳定后再做前端。
2. 先完成核心闭环，再扩展高级功能。
3. 用户数据必须隔离。
4. AI 结果必须允许修改。
5. 原始输入必须保存。
6. 统计逻辑以后端为准。
7. 页面要简洁，不做复杂后台风格。
8. 安慰反馈要温暖，不能制造压力。
9. 代码结构要清晰，方便面试讲解。
10. 项目要真实可用，不只是模板项目。

---

## 20. 总结

StudyMate 的核心价值不是简单打卡，而是帮助 Java 实习备考用户完成学习复盘。

第一版开发重点是：

    能记录
    能整理
    能保存
    能统计
    能分析薄弱点
    能给出建议和安慰

开发时不要盲目堆功能，应优先保证核心流程完整、后端接口稳定、数据权限安全、AI 整理可用。

这个项目应该体现出真实需求和 Java 后端能力，适合作为 Java 实习项目进行展示。